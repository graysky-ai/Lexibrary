// Mixed module-level constants: primitives (indexed), object/arrow (skipped).

const API_URL = "https://api.example.com";
const MAX_RETRIES = 5;
const config = { timeout: 30 };
const handler = () => {};
