comments:
- body: New module implementing the Deprecation Analyst sub-agent wrapper (curator-2,
    group 4). Provides input sanitisation to prevent template injection via Jinja2
    syntax in artifact content, and an async BAML function wrapper following the ArchivistService
    pattern. The needs_human_review() function strips code fences before checking
    for suspicious patterns, ensuring template syntax inside code blocks is not falsely
    flagged.
  date: '2026-04-09T06:54:56.323078Z'
