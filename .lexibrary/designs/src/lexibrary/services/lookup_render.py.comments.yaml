comments:
- body: "Added render_key_symbols() which formats KeySymbolSummary lists into a '###\
    \ Key symbols' markdown table via cli/_output.markdown_table. Methods render as\
    \ the last two dotted segments of qualified_name ('Class.method') so readers can\
    \ see the owning class without scrolling up; functions and classes render as their\
    \ bare name. Counts render as 'callers \u2192 callees' using a U+2192 arrow. When\
    \ key_symbols_total exceeds the rendered row count, a trailing '\u2026 and N more'\
    \ overflow marker is appended. Returns '' on empty input so the CLI can omit the\
    \ section entirely."
  date: '2026-04-11T05:24:00.215747Z'
- body: 'symbol-graph-3 Group 6: Added render_class_hierarchy() which emits a ''###
    Class hierarchy'' markdown table with columns Class | Bases | Subclasses | Methods
    | Line. Resolved bases join with '', ''; unresolved bases are appended with a
    trailing ''*'' marker (e.g. ''BaseModel*'') so readers can visually distinguish
    in-scope inheritance from out-of-scope external bases like pydantic.BaseModel.
    Returns an empty string when classes is empty so the CLI can omit the whole section
    without a conditional there.'
  date: '2026-04-11T17:21:13.111593Z'
- body: 'symbol-graph-5 Group 7: added pure render_enum_notes() and render_call_path_notes()
    helpers that emit ''### Enums & constants'' and ''### Call paths'' sections from
    the parsed DesignFile enrichment fields. Both follow the module convention of
    returning a string (no direct info() calls); the CLI handler in lexi_app.py prints
    them after render_class_hierarchy in full-mode lookup output.'
  date: '2026-04-11T23:43:13.331992Z'
